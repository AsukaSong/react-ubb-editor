import { faAlignLeft } from '@fortawesome/free-solid-svg-icons'
import {
  IUBBButtonConfig,
} from '../../types'

export default <IUBBButtonConfig>{
  type: 'button',
  tagName: 'left',
  title: '左对齐',
  icon: faAlignLeft,
  index: 50,
}
